---
import { getCollection, render, type CollectionEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
	const entries = await getCollection('research');
	return entries
		.filter((entry) => entry.data.visibility === 'public' && entry.data.status !== 'draft')
		.map((entry) => ({
			params: { slug: entry.data.slug },
			props: { entry },
		}));
}

const { entry } = Astro.props as { entry: CollectionEntry<'research'> };
const { Content } = await render(entry);
---

<BaseLayout title={entry.data.title} description={entry.data.summary}>
	<section class="mx-auto max-w-[920px] space-y-8 pt-6">
		<a href="/research" class="section-label inline-flex items-center gap-3 no-underline transition hover:text-rust">
			<span aria-hidden="true">←</span>
			Back to research
		</a>

		<header class="border-t border-black/80 pt-6">
			<p class="section-label">{entry.data.category.replaceAll('_', ' ')}</p>
			<h1 class="protocol-title mt-5 text-[3rem] leading-[0.92] sm:text-[5rem]">{entry.data.title}</h1>
			<div class="mt-6 flex flex-wrap gap-2 font-mono text-[0.68rem] uppercase text-ash">
				<span class="border border-black/20 px-3 py-1">{entry.data.year}</span>
				<span class="border border-black/20 px-3 py-1">{entry.data.status.replaceAll('_', ' ')}</span>
				{entry.data.themes.map((theme) => <span class="border border-rust/40 px-3 py-1 text-rust">{theme}</span>)}
			</div>
			<p class="mt-6 max-w-3xl text-[1.08rem] leading-8 text-ink/80">{entry.data.summary}</p>
		</header>

		<article class="copy-prose surface-card px-6 py-7 sm:px-8 sm:py-9">
			<Content />
		</article>
	</section>
</BaseLayout>
